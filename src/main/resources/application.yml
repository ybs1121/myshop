spring:
  application:
    name: MyShop
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/myshop?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: admin
    password: admin
  jpa:
    hibernate:
      ddl-auto: update # ?? ????? update, ?? ????? validate? none ??
    show-sql: true # ??? SQL ??? ??? ??
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect # MySQL 8.x ??? Dialect
